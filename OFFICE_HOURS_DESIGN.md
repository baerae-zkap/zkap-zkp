# Design: zkap-zkp SDK 오픈소스 패키징 및 릴리스 아키텍처

Generated by /office-hours on 2026-04-06
Branch: unknown
Repo: zkap-zkp
Status: DRAFT
Mode: Builder (Open Source / Architecture)

---

## 전제 (Premises) — Phase 3

이 설계가 의존하는 핵심 전제들.

1. **Node.js 바이너리는 napi-rs 패턴으로 배포한다.** — napi-rs가 optionalDependencies + 플랫폼 패키지 구조를 자동 생성하며, 이 에코시스템이 가장 검증됐다.
2. **WASM은 hash 3종만 지원한다.** — 브라우저에서 대용량 proving key를 로드하는 것은 현실적으로 불가능하다. 100MB+ 파일을 WebAssembly 메모리에 올리는 사용자 경험은 없다.
3. **React Native의 네이티브 모듈 링킹은 JS 레이어와 분리된 문제다.** — `exports` 조건으로 JS 라우팅은 해결할 수 있지만, CocoaPods/Gradle 링킹은 별도 패키지 설치를 요구한다.
4. **CRS/PK/VK는 npm 외부에 호스팅한다.** — GitHub Releases를 1차 호스팅으로 쓰고, 무결성 검증은 SHA256으로 한다.
5. **단일 facade(`@scope/sdk`)는 Node.js와 브라우저에는 가능하지만, React Native에는 별도 패키지가 필요하다.** — 이것이 유지보수성과 사용자 경험 사이의 핵심 trade-off다.

---

## A. Executive Summary

**추천 아키텍처: 메타 패키지 + 런타임별 분리 패키지**

`@zkap/sdk`를 단일 facade 패키지로 두고, 내부적으로 런타임별 패키지로 위임한다.

- **Node.js 사용자**: `npm install @zkap/sdk` 하나로 완료. 내부에서 napi-rs platform package를 `optionalDependencies`로 자동 선택.
- **브라우저(번들러) 사용자**: `npm install @zkap/sdk-wasm` 직접 설치. wasm-pack 산출물.
- **React Native 사용자**: `npm install @zkap/sdk-react-native`. Expo Modules API 기반 네이티브 모듈. 별도 패키지가 불가피하다.

CRS/PK/VK는 npm에 포함 금지. GitHub Releases에 호스팅하고, 런타임 초기화 시 다운로드 + SHA256 검증 후 로컬 캐시.

Rust core repo는 별도 public repo로 유지하고, bindings repo(이 설계의 대상)는 두 번째 public mono-repo로 운영한다.

---

## B. Capability Matrix

| 함수 | Node.js | WebAssembly | React Native | 이유 |
|------|---------|-------------|--------------|------|
| `generate_anchor` | ✅ | ❌ | ❌ | 서버사이드 앵커 생성. 브라우저/모바일에선 불필요한 서버 전용 연산. |
| `generate_aud_hash` | ✅ | ✅ | ✅ | 순수 해시. 어디서든 가볍게 실행 가능. |
| `generate_hash` | ✅ | ✅ | ✅ | 순수 해시. |
| `generate_leaf_hash` | ✅ | ✅ | ✅ | 순수 해시. |
| `groth16_setup` | ✅ | ❌ | ❌ | Setup은 프로토콜 관리자가 1회 수행. 대용량 CRS 필요. 모바일/브라우저 불가. |
| `prove` | ✅ | ❌ | ✅ | Node: 서버 증명. RN: 온디바이스 증명(오프라인 ZK). WASM: 메모리/proving key 로드 현실 불가. |
| `verify` | ✅ | ❌ | ❌ | 검증은 서버가 담당하는 것이 일반적. 모바일에선 verifying key 배포 복잡도 증가. |

### Unsupported API 에러 처리

```typescript
// @zkap/sdk-wasm 예시
export function prove(): never {
  throw new Error(
    '[zkap/sdk-wasm] prove() is not supported in WebAssembly. ' +
    'Use @zkap/sdk (Node.js) for server-side proving, ' +
    'or @zkap/sdk-react-native for on-device proving.'
  );
}
```

TypeScript 타입 레벨에서도 런타임별 타입 정의를 분리해 IDE에서 즉시 확인 가능하도록 한다.

---

## C. Recommended Package Topology

### Public npm packages (공개 배포)

| 패키지 이름 | 책임 | 비고 |
|------------|------|------|
| `@zkap/sdk` | Node.js/브라우저 facade. `exports` 조건으로 런타임 자동 선택. | 메인 엔트리포인트 |
| `@zkap/sdk-node` | Node.js napi-rs 바인딩. optionalDependencies로 platform package 선택. | proof feature 포함 |
| `@zkap/sdk-wasm` | WebAssembly wasm-pack 산출물. hash 3종만. | bundler/browser 타겟 |
| `@zkap/sdk-react-native` | Expo Modules API 기반 네이티브 모듈. iOS + Android pre-built Rust lib 포함. | New Architecture 전용 |
| `@zkap/node-darwin-x64` | macOS x64 napi-rs 바이너리 (`*.node`) | auto-generated by napi-rs |
| `@zkap/node-darwin-arm64` | macOS arm64 napi-rs 바이너리 | auto-generated by napi-rs |
| `@zkap/node-linux-x64-gnu` | Linux x64 glibc napi-rs 바이너리 | auto-generated by napi-rs |
| `@zkap/node-linux-x64-musl` | Linux x64 musl (Alpine) napi-rs 바이너리 | auto-generated by napi-rs |

### Internal / build-only (내부 전용, npm 미배포)

| 패키지 이름 | 책임 |
|------------|------|
| `@zkap/rust-core-bindings` | Rust FFI 정의. napi-rs + wasm-bindgen 설정. 빌드 스크립트. |
| `@zkap/typescript-shared` | 공통 TypeScript 타입 정의. 에러 타입. 각 패키지에 번들링 후 배포. |
| `@zkap/artifact-manager` | CRS/PK/VK 다운로드/캐시/검증 로직. Node + RN 각각 플랫폼별 구현. |

---

## D. Install / Import UX Design

### Node.js 사용자

```bash
npm install @zkap/sdk
```

`@zkap/sdk`의 `package.json`:
```json
{
  "name": "@zkap/sdk",
  "version": "1.0.0",
  "exports": {
    ".": {
      "node": "./dist/node.js",
      "browser": "./dist/browser.js",
      "default": "./dist/node.js"
    },
    "./artifact-manager": {
      "node": "./dist/artifact-manager/node.js",
      "default": "./dist/artifact-manager/node.js"
    }
  },
  "dependencies": {
    "@zkap/sdk-node": "^1.0.0"
  }
}
```

`dist/node.js`:
```typescript
export {
  generateAnchor,
  generateAudHash,
  generateHash,
  generateLeafHash,
  groth16Setup,
  prove,
  verify,
} from '@zkap/sdk-node';
```

사용 예시:
```typescript
import { generateHash, prove } from '@zkap/sdk';
import { initArtifacts } from '@zkap/sdk/artifact-manager';

await initArtifacts(); // CRS 다운로드 및 캐시 확인
const proof = await prove(request);
```

### 브라우저(번들러) 사용자

```bash
npm install @zkap/sdk-wasm
```

`@zkap/sdk`의 `browser` exports condition으로도 접근 가능하나, 브라우저 사용자에게는 `@zkap/sdk-wasm` 직접 설치를 권장. `@zkap/sdk`가 WASM 번들을 dependency로 포함하면 Node.js 사용자가 불필요한 WASM 바이너리를 다운받는 문제 발생.

```typescript
import init, { generateHash } from '@zkap/sdk-wasm';

await init(); // WASM 초기화
const hash = generateHash(input);
```

### React Native 사용자

```bash
npm install @zkap/sdk-react-native
cd ios && pod install  # iOS 네이티브 링킹
```

```typescript
import { generateHash, prove } from '@zkap/sdk-react-native';
import { initProveArtifacts } from '@zkap/sdk-react-native/artifact-manager';

// 앱 시작 시
await initProveArtifacts({
  onProgress: (downloaded, total) => { /* 진행률 UI */ },
  cacheDir: FileSystem.cacheDirectory + 'zkap/',
});

const hash = await generateHash(input);
const proof = await prove(request);
```

### 단일 facade 가능 여부 — 솔직한 평가

**Node.js + 브라우저: 가능.** `exports` 조건으로 깔끔하게 분기.

**React Native: 불가능, 이유는 구조적이다.**

React Native 네이티브 모듈은 JS `require`/`import` 시점에 동적으로 바이너리를 로드하는 것이 아니라, iOS CocoaPods / Android Gradle이 빌드 타임에 링킹해야 한다. `@zkap/sdk`에 `react-native` exports 조건을 추가해도, 그것이 참조하는 네이티브 코드(`ios/`, `android/` 디렉토리 + pre-built `.a`/`.so` 파일)가 실제로 `node_modules/@zkap/sdk-react-native`에 존재해야 한다. 따라서 RN 사용자는 반드시 `@zkap/sdk-react-native`를 별도로 설치해야 한다.

이것은 `react-native-reanimated`, `react-native-mmkv` 등 모든 RN 네이티브 모듈이 따르는 동일한 제약이다. "RN은 별도 패키지"는 버그가 아니라 생태계 표준이다.

---

## E. Artifact Distribution Strategy

### npm에 포함되는 것

| 아이템 | 위치 | 설명 |
|--------|------|------|
| napi-rs `.node` 바이너리 | `@zkap/node-{platform}` 각 패키지 | 5~15MB, 플랫폼별 분리로 사용자는 1개만 다운 |
| WASM 바이너리 + JS glue | `@zkap/sdk-wasm` | wasm-pack 산출물, ~2~5MB |
| iOS pre-built XCFramework | `@zkap/sdk-react-native/ios/` | arm64 + simulator 슬라이스 포함 |
| Android pre-built `.so` | `@zkap/sdk-react-native/android/` | arm64-v8a, armeabi-v7a, x86_64 |
| Artifact manifest JSON | 각 패키지 `assets/artifact-manifest.json` | CRS/PK/VK URL + SHA256, 버전 정보 |
| TypeScript 타입 정의 | 모든 패키지 | `.d.ts` 파일 |

### npm 외부에 호스팅되는 것

| 아이템 | 호스팅 | 포맷 |
|--------|--------|------|
| Proving Key (PK) | GitHub Releases (primary) + 선택적으로 CDN | `.bin.zst` (zstd 압축) |
| Verifying Key (VK) | GitHub Releases | `.bin` (소용량, 수KB) |
| Setup Artifact (CRS) | GitHub Releases | `.bin.zst` |
| Manifest 최신본 | GitHub Releases + 선택적으로 CDN | `manifest.json` |

### Artifact Manifest 구조

```json
{
  "version": "1.0.0",
  "circuit": "zkap-v1",
  "artifacts": {
    "proving_key": {
      "url": "https://github.com/org/zkap-zkp/releases/download/v1.0.0/pk.bin.zst",
      "sha256": "abc123...",
      "size_bytes": 104857600,
      "compressed_size_bytes": 52428800
    },
    "verifying_key": {
      "url": "https://github.com/org/zkap-zkp/releases/download/v1.0.0/vk.bin",
      "sha256": "def456...",
      "size_bytes": 2048
    }
  },
  "min_sdk_version": "1.0.0",
  "max_sdk_version": "1.x.x"
}
```

### 런타임별 다운로드/캐시 전략

**Node.js:**
```
캐시 경로: $XDG_CACHE_HOME/zkap/ 또는 ~/.cache/zkap/
파일명: zkap-v1.0.0-pk.bin (버전 포함)
초기화: SDK 로드 시 자동 확인. 없으면 다운로드 프롬프트 또는 자동 다운.
스트리밍 다운로드 → SHA256 실시간 검증 → 완료 시 캐시 기록.
```

**React Native:**
```
캐시 경로: FileSystem.cacheDirectory + 'zkap/'
(iOS: <app>/Library/Caches/zkap/)
(Android: <app>/cache/zkap/)
초기화: 앱 첫 실행 시 또는 증명 기능 첫 접근 시.
expo-file-system으로 스트리밍 다운로드.
진행률 콜백으로 UI 표시 가능.
백그라운드 다운로드 API 활용 권장 (iOS Background URL Session).
```

### 무결성 검증

```
1. manifest.json 다운로드 (HTTPS로 transport-layer 보호)
2. 아티팩트 스트리밍 다운로드
3. 다운로드 중 SHA256 rolling hash 계산
4. 완료 후 manifest의 sha256과 비교
5. 불일치 시 → 임시 파일 삭제 → 에러 throw
6. 검증 통과 시 → 최종 경로로 atomic move
```

### 버전 호환성 전략

- SDK 버전 v1.x.x는 circuit v1 아티팩트만 사용.
- Major 버전 업(v2.x.x)은 새 circuit과 새 아티팩트 요구.
- Manifest에 `min_sdk_version` / `max_sdk_version` 명시.
- 호환 불가 시 명확한 에러 메시지: `"zkap SDK v2.0.0은 circuit v1 아티팩트와 호환되지 않습니다. SDK를 업그레이드하거나 v1 아티팩트를 사용하세요."`

### 실패 시 복구 전략

```
1. 다운로드 실패 → 최대 3회 재시도 (exponential backoff)
2. 부분 다운로드 → 이어받기 지원 (HTTP Range 헤더)
3. 검증 실패 → 캐시 삭제 후 재다운로드
4. 캐시 손상 → 앱 시작 시 캐시 무결성 사전 확인
5. 디스크 공간 부족 → 명확한 에러 + 필요 용량 안내
6. GitHub Releases 다운 → 백업 URL 설정 가능 (manifest에 mirror 필드 추가)
```

---

## F. Repo Layout

### 저장소 구조

**Rust core repo (기존, 별도 public)**
```
zkap-core/  (별도 public repo, 변경 없음)
└── src/
    ├── anchor/
    ├── circuit/
    ├── hash/
    └── proof/
```

**Bindings repo (이번 설계 대상, public)**
```
zkap-sdk/
├── package.json                    ← npm workspaces 루트
├── .npmrc                          ← @zkap 레지스트리 설정
├── Cargo.toml                      ← workspace Cargo.toml (zkap-core를 path 또는 git dep으로)
├── Cargo.lock
│
├── packages/
│   ├── sdk/                        ← @zkap/sdk (facade, Node+browser)
│   │   ├── package.json
│   │   ├── src/
│   │   │   ├── node.ts
│   │   │   └── browser.ts
│   │   └── assets/
│   │       └── artifact-manifest.json
│   │
│   ├── sdk-node/                   ← @zkap/sdk-node (napi-rs)
│   │   ├── package.json
│   │   ├── src/
│   │   │   └── lib.rs              ← #[napi] 어노테이션
│   │   ├── build.rs
│   │   └── __test__/
│   │
│   ├── sdk-wasm/                   ← @zkap/sdk-wasm (wasm-pack)
│   │   ├── package.json
│   │   ├── src/
│   │   │   └── lib.rs              ← #[wasm_bindgen] 어노테이션
│   │   ├── Cargo.toml
│   │   └── pkg/                    ← wasm-pack 빌드 산출물 (gitignored)
│   │
│   └── sdk-react-native/           ← @zkap/sdk-react-native
│       ├── package.json
│       ├── src/
│       │   └── index.ts            ← Expo Modules API JS/TS 인터페이스
│       ├── ios/
│       │   ├── ZkapSdkModule.swift ← ExpoModule Swift 구현
│       │   ├── ZkapSdkModule.podspec
│       │   └── lib/
│       │       ├── libzkap_core_ios_arm64.a
│       │       └── libzkap_core_ios_sim_arm64.a
│       ├── android/
│       │   ├── src/main/java/expo/modules/zkap/
│       │   │   └── ZkapSdkModule.kt
│       │   ├── build.gradle
│       │   └── libs/
│       │       ├── arm64-v8a/libzkap_core.so
│       │       ├── armeabi-v7a/libzkap_core.so
│       │       └── x86_64/libzkap_core.so
│       └── expo-module.config.json
│
├── platform-packages/              ← napi-rs auto-generated
│   ├── node-darwin-x64/
│   ├── node-darwin-arm64/
│   ├── node-linux-x64-gnu/
│   └── node-linux-x64-musl/
│
├── examples/
│   ├── node-example/               ← Node.js 사용 예제 (패키지)
│   │   ├── package.json
│   │   └── src/index.ts
│   ├── browser-example/            ← Vite 기반 브라우저 예제
│   │   ├── package.json
│   │   └── src/
│   └── expo-example/               ← Expo Bare 예제 앱
│       ├── app.json
│       ├── package.json
│       └── app/
│
├── docs/
│   ├── CAPABILITY_MATRIX.md
│   ├── SUPPORT_MATRIX.md
│   ├── RELEASE.md
│   ├── ARTIFACT_HOSTING.md
│   └── CONTRIBUTING.md
│
└── .github/
    └── workflows/
        ├── build-node.yml          ← Node.js 플랫폼 바이너리 빌드
        ├── build-wasm.yml          ← WASM 빌드
        ├── build-react-native.yml  ← iOS/Android 네이티브 라이브러리 빌드
        ├── release.yml             ← 태그 릴리스 트리거
        └── ci.yml                 ← PR 검증 (테스트, 타입체크)
```

### Cargo.toml workspace 관계

zkap-core는 zkap-sdk의 Cargo dependency로 참조. 로컬 개발 시 path dep, CI는 git rev 또는 crates.io 버전.

```toml
[workspace]
members = [
  "packages/sdk-node",
  "packages/sdk-wasm",
  "packages/sdk-react-native/rust",
]

[workspace.dependencies]
zkap-core = { git = "https://github.com/org/zkap-core", tag = "v1.0.0" }
```

---

## G. Release & CI Design

### 빌드 매트릭스

| 빌드 타겟 | 실행 환경 | 도구 | 산출물 |
|----------|----------|------|--------|
| Node.js darwin-x64 | macos-latest (GitHub Actions) | napi-rs, cross-compile 불필요 | `*.node` |
| Node.js darwin-arm64 | macos-latest (Apple Silicon runner) | napi-rs | `*.node` |
| Node.js linux-x64-gnu | ubuntu-latest | napi-rs | `*.node` |
| Node.js linux-x64-musl | ubuntu-latest + Docker (Alpine) | napi-rs + `cross` 또는 `cargo-zigbuild` | `*.node` |
| WASM | ubuntu-latest | wasm-pack, wasm-opt | `*.wasm` + JS glue |
| iOS (arm64 device) | macos-latest | cargo target `aarch64-apple-ios` | `libzkap_core.a` |
| iOS (arm64 simulator) | macos-latest | cargo target `aarch64-apple-ios-sim` | `libzkap_core.a` |
| Android arm64-v8a | ubuntu-latest | cargo + NDK, `aarch64-linux-android` | `libzkap_core.so` |
| Android armeabi-v7a | ubuntu-latest | cargo + NDK, `armv7-linux-androideabi` | `libzkap_core.so` |
| Android x86_64 | ubuntu-latest | cargo + NDK, `x86_64-linux-android` | `libzkap_core.so` |

### linux-x64-musl 빌드 (Alpine 대응)

```yaml
# build-node.yml 발췌
- name: Build linux-x64-musl
  uses: actions/cache@v4
  ...
- name: Build with cargo-zigbuild
  run: |
    cargo install cargo-zigbuild
    cargo zigbuild --target x86_64-unknown-linux-musl --release
```

`cargo-zigbuild`가 musl cross-compile을 가장 깔끔하게 처리한다. Docker Alpine 컨테이너보다 CI 속도 빠름.

### 태그 릴리스 흐름

```
git tag v1.0.0 && git push origin v1.0.0
↓
release.yml 트리거 (on: push: tags: ['v*'])
↓
[병렬] 전체 플랫폼 바이너리 빌드 (Node x4, WASM, iOS x2, Android x3)
↓
GitHub Release draft 생성
↓
CRS/PK/VK 아티팩트를 Release assets으로 업로드 (별도 보관소에서 가져옴)
↓
manifest.json 생성 (URL + SHA256 자동 계산) → Release asset으로 업로드
↓
각 npm 패키지 tarball 생성
↓
npm publish (workspaces, --access public)
↓
Release 확정 (draft → published)
```

### GitHub Releases 활용

- **모든 바이너리 + 아티팩트**: GitHub Releases assets으로 호스팅.
- **CHANGELOG**: 각 Release 본문에 자동 생성 (`git cliff` 또는 `conventional-changelog`).
- **CRS/PK/VK**: 릴리스 태그에 묶어 관리. SDK 버전과 아티팩트 버전이 1:1 대응.

### npm publish 흐름

```yaml
- name: Publish npm packages
  run: |
    # platform packages 먼저 (순서 중요)
    npm publish ./platform-packages/node-darwin-x64 --access public
    npm publish ./platform-packages/node-darwin-arm64 --access public
    npm publish ./platform-packages/node-linux-x64-gnu --access public
    npm publish ./platform-packages/node-linux-x64-musl --access public
    # 주요 패키지
    npm publish ./packages/sdk-node --access public
    npm publish ./packages/sdk-wasm --access public
    npm publish ./packages/sdk-react-native --access public
    # facade 마지막
    npm publish ./packages/sdk --access public
  env:
    NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
```

플랫폼 패키지를 facade보다 먼저 publish하지 않으면 optionalDependencies resolve 실패.

### 실패 복구

- **플랫폼 바이너리 빌드 실패**: 해당 플랫폼만 재트리거 가능하도록 workflow를 분리 설계.
- **npm publish 부분 실패**: `npm unpublish`는 24시간 내 가능. 실패한 패키지만 재publish.
- **Release 아티팩트 누락**: GitHub API로 개별 asset upload 재시도 가능.

---

## H. Risks / Trade-offs

### 크기

| 리스크 | 평가 | 완화 |
|--------|------|------|
| `@zkap/sdk-react-native`에 iOS/Android pre-built lib 포함 | iOS `.a` 파일들이 50~200MB 가능. npm tarball 크기 이슈. | XCFramework 대신 `.xcframework`로 묶어 slicing 활성화. Android는 ABI-split된 `.aar`. npm tarball은 `files` 필드로 정밀 제어. |
| WASM 바이너리 크기 | 최적화 없이 5~20MB 가능 | `wasm-opt -Os` + `wasm-pack` release 빌드. hash만 컴파일해 크기 최소화. |

### 설치 복잡도

| 리스크 | 평가 |
|--------|------|
| RN 사용자가 `pod install` 누락 | Expo Modules 문서화 표준이나, 첫 설치 경험에서 자주 실수. postinstall 스크립트로 경고 메시지 출력. |
| optionalDependencies platform mismatch | Windows 등 지원 외 플랫폼에서 설치 시 바이너리 없음. 명확한 에러 메시지 필수. |

### React Native 유지보수

| 리스크 | 평가 |
|--------|------|
| New Architecture만 지원 (구 아키텍처 제외) | 아직 구 아키텍처 사용자 비중 있음. 하지만 요구사항이 New Architecture 전용이므로 수용. |
| Rust → iOS/Android 크로스컴파일 CI 비용 | macos runner가 비쌈. 월 CI 비용 주의. 캐싱 적극 활용. |
| Expo SDK 버전 업에 따른 Expo Modules API 변경 | Expo Modules API는 비교적 안정적이나 major 버전마다 검토 필요. |

### WASM 제약

| 리스크 | 평가 |
|--------|------|
| WASM에서 prove 미지원 | 브라우저 ZK 증명 수요가 생길 경우 대응 어려움. 현 시점에서는 의도된 제약. |
| wasm-bindgen / wasm-pack 생태계 변화 | wasm-pack은 안정적이나 활발한 개발이 줄었음. 차세대 도구(component model) 모니터링 필요. |

### Proof Portability

| 리스크 | 평가 |
|--------|------|
| PK 파일이 100MB+ | GitHub Releases asset 크기 제한(2GB)은 문제 없으나, 다운로드 경험 주의. 진행률 UI 필수. |
| PK 버전 불일치 | circuit 업그레이드 시 기존 캐시 무효화 로직 필수. manifest의 버전 필드로 관리. |
| CDN 없이 GitHub Releases만 사용 시 글로벌 다운로드 속도 | GitHub의 CDN이 상당히 빠르지만, 국가별 편차. 필요 시 Cloudflare R2 또는 S3 mirror 추가. |

### ABI / Binary Compatibility

| 리스크 | 평가 |
|--------|------|
| napi-rs ABI 버전 | N-API(Node-API)는 Node.js LTS 전반에서 ABI 안정적. napi-rs v2 유지하면 문제 없음. |
| glibc 버전 | ubuntu-latest runner의 glibc 버전 고정 필요. `manylinux` 컨테이너 사용 검토. |

### Artifact Hosting Risks

| 리스크 | 완화 |
|--------|------|
| GitHub Releases 서비스 장애 | manifest에 mirror URL 필드 지원. |
| 아티팩트 URL 영구 삭제 | Release를 절대 삭제하지 않는 정책. SDK 코드에서 특정 Release 태그 URL을 하드코딩하지 않고 manifest 경유. |

---

## I. Recommendation

### 지금 추천

1. **napi-rs로 Node.js 바인딩 시작.** `napi-rs/package-template` 템플릿에서 시작하면 platform package 구조, CI workflow, optionalDependencies 설정이 모두 자동 생성된다.

2. **`@zkap/sdk-node`, `@zkap/sdk-wasm`, `@zkap/sdk-react-native` 3개 패키지로 시작.** `@zkap/sdk` facade는 Node/browser가 안정화된 후에 추가해도 늦지 않다.

3. **React Native는 Expo Modules API + pre-built Rust static lib.** Rust를 사용자 빌드 환경에서 컴파일하게 하지 말 것. CI에서 미리 빌드해 npm에 포함.

4. **GitHub Releases를 아티팩트 1차 호스팅으로.** 별도 인프라 없이 시작 가능. CDN은 다운로드 병목이 실제로 발생할 때 추가.

5. **Manifest 기반 아티팩트 관리.** `artifact-manifest.json`을 각 SDK 버전에 번들하고, 런타임에 다운로드 + SHA256 검증. 이 로직을 초기에 제대로 만들면 이후 CI에서 자동화 가능.

6. **CI 비용 최적화**: 바이너리 빌드는 `on: push: tags` 또는 `on: workflow_dispatch`로만 실행. PR CI는 빌드 없이 `cargo check`, `cargo test`, TypeScript 타입체크만.

### 나중에 허용 가능

- **WASM에서 verify 지원**: VK 파일이 작으므로 (수KB), 브라우저에서 검증만 지원하는 것은 나중에 추가 가능.
- **`@zkap/sdk` facade에 RN 포함**: RN 네이티브 코드를 facade에 직접 넣으면 단일 설치 가능. 하지만 Node.js 사용자가 iOS/Android 바이너리를 다운받게 되는 trade-off. 사용자 기반이 충분히 커졌을 때 UX 개선으로 고려.
- **CDN mirror**: 글로벌 사용자 늘어나면 Cloudflare R2 또는 S3 mirror 추가.
- **wasm-pack → wasm-component-model**: WebAssembly Component Model 생태계가 성숙하면 마이그레이션.
- **musl 빌드 추가 아키텍처**: linux arm64 musl (ARM 서버/IoT) 수요 발생 시.

### 피해야 할 방식

- **모든 바이너리를 `@zkap/sdk` 단일 패키지에 포함.** WASM + iOS + Android + 4개 Node 바이너리를 한 패키지에 넣으면 npm tarball이 500MB+가 될 수 있다. 설치마다 전부 다운받는다. 이건 npm의 존재 이유를 부정하는 패키지다.

- **Rust 소스 코드를 React Native 패키지에 포함하고 사용자 환경에서 컴파일.** RN 앱 개발자 머신에 Rust toolchain 설치를 요구하는 순간 채택률이 급락한다.

- **CRS/PK를 npm에 포함.** 100MB+ 파일을 npm registry에 올리면 publish 느리고, 사용자 설치 느리고, npm 정책 위반 가능성 있다.

- **`npm install @scope/sdk`가 React Native에서도 바로 동작한다고 문서화.** 동작하지 않는다. 잘못된 기대를 설정하면 이슈 트래커가 "pod install 안 했어요"로 가득 찬다.

- **단일 GitHub Actions workflow에 모든 빌드 포함.** 특정 플랫폼 빌드 실패 시 전체 재실행이 필요해 CI 비용 낭비. 빌드 타겟별로 workflow 분리.

- **proof 관련 기능을 별도 npm 패키지로 분리.** ("hash 패키지"와 "proof 패키지"를 나누는 방식) 이건 오히려 사용자가 두 패키지를 관리해야 하는 부담을 준다. 런타임별 분리가 올바른 경계다. 기능별 분리는 아니다.

---

## Open Questions

1. `@zkap` npm scope 소유권 확인 — 현재 점유 여부 확인 필요.
2. iOS Simulator에서 arm64와 x86_64 슬라이스 모두 지원할지 결정 (x86_64 Mac 개발자 지원 범위).
3. React Native에서 `prove` 실행 시 UI 스레드 블로킹 방지 전략 — Expo Modules API는 기본적으로 비동기 실행 지원하나, 증명 시간이 수초 이상이면 별도 스레드 명시적 설정 필요.
4. Groth16 proving key 파일 크기 실측 — 설계에서 100MB를 가정했으나 실제 회로 크기에 따라 다름. 크기에 따라 압축 전략과 캐시 정책 재검토 필요.
5. VK 소용량 확인 시 — VK가 수KB 이하면 npm 패키지에 직접 번들하는 것도 고려 가능.

---

## 참고 패키지 예시

napi-rs 패턴의 실제 예시:
- `@napi-rs/snappy` — napi-rs multi-platform 패키지의 교과서적 구조
- `@swc/core` — SWC의 플랫폼 분리 패키지 구조 (동일 패턴)

Expo Modules API 패턴:
- `expo-camera`, `expo-av` — Expo 공식 네이티브 모듈 구조 참조

WASM 배포 패턴:
- `@tensorflow/tfjs-backend-wasm` — WASM 바이너리 배포 방식 참조

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 0 | — | — |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |

**VERDICT:** NO REVIEWS YET — run `/plan-eng-review` for architecture deep-dive.
