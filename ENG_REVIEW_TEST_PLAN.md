# Test Plan — zkap-zkp SDK

Generated by /plan-eng-review on 2026-04-06
Branch: unknown
Repo: zkap-zkp

---

## Affected Packages

- `@baerae/zkap-zkp-node` — Node.js napi-rs 바인딩
- `@baerae/zkap-zkp-wasm` — WebAssembly wasm-bindgen 바인딩
- `@baerae/zkap-zkp-react-native` — Expo Modules API 네이티브 모듈
- Artifact Manager (Node + RN 공통)
- CI 빌드 매트릭스

---

## Key Interactions to Verify

### @baerae/zkap-zkp-node
- [ ] napi-rs `.node` 바이너리 로드 (각 플랫폼별)
- [ ] `generate_anchor` — happy path, 잘못된 입력 에러
- [ ] `generate_aud_hash`, `generate_hash`, `generate_leaf_hash` — happy path + 경계값
- [ ] `prove()` — happy path, artifact 미초기화 시 명확한 에러
- [ ] `verify()` — happy path, 잘못된 proof 입력
- [ ] `groth16_setup()` — 로컬 전용, CI에서 실행 안 함

### @baerae/zkap-zkp-wasm
- [ ] `init()` — WASM 로드 성공
- [ ] `generate_anchor`, hash 3종 — happy path
- [ ] `prove()` 호출 시 명확한 에러 메시지 throw 확인
- [ ] Bundler 환경 (Vite, webpack) 에서 lazy import 동작

### @baerae/zkap-zkp-react-native (실기기 필수)
- [ ] [→E2E] iOS arm64 실기기 — `generateHash`, `prove`
- [ ] [→E2E] Android arm64-v8a 실기기 — `generateHash`, `prove`
- [ ] `initProveArtifacts()` — 진행률 콜백 정상 동작
- [ ] `initProveArtifacts()` — 네트워크 실패 시 재시도 (3회)
- [ ] `initProveArtifacts()` — 이어받기 (HTTP Range)
- [ ] `prove()` 실행 중 앱 백그라운드 전환 후 복귀 처리

---

## Edge Cases

### Artifact Manager
- [ ] manifest.json 다운로드 실패 → 3회 재시도 후 에러
- [ ] SHA256 검증 실패 → 임시 파일 삭제 확인
- [ ] 부분 다운로드 후 앱 재시작 → HTTP Range 이어받기
- [ ] atomic rename 확인 (다운로드 중 앱 강제 종료 후 재시작 시 캐시 손상 없음)
- [ ] 디스크 공간 부족 시 명확한 에러 + 필요 용량 안내

### npm publish 검증
- [ ] `npm publish --dry-run` 8개 패키지 전체 성공
- [ ] platform package publish 순서 (facade보다 먼저)
- [ ] optionalDependencies resolve 확인 (각 플랫폼에서 올바른 바이너리 선택)

### CI 빌드 매트릭스
- [ ] `linux-x64-musl`: cargo-zigbuild로 빌드 성공
- [ ] `darwin-arm64`: Apple Silicon runner에서 빌드 성공
- [ ] WASM: `wasm-opt -Os` 적용 후 크기 확인 (목표: < 5MB)

---

## Critical Paths

1. Node.js: `npm install @baerae/zkap-zkp` → `prove()` 실행까지 end-to-end
2. WASM: `npm install @baerae/zkap-zkp-wasm` → 브라우저에서 `generateHash()` 실행
3. React Native: `npm install @baerae/zkap-zkp-react-native` + `pod install` → 실기기 `prove()` 실행
4. Artifact: PK 다운로드 → SHA256 검증 → 캐시 저장 → 재사용 (두 번째 실행 시 다운로드 없음)

---

## Open Questions (구현 전 결정 필요)

- iOS Simulator arm64 외 x86_64 슬라이스 지원 여부
- React Native `prove()` 실행 스레드 명시적 설정 필요 여부
- Groth16 proving key 실제 파일 크기 실측 후 압축 전략 재검토
